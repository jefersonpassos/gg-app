import { StoreModule } from './store.module';
describe('StoreModule', function () {
    var storeModule;
    beforeEach(function () {
        storeModule = new StoreModule();
    });
    it('should create an instance', function () {
        expect(storeModule).toBeTruthy();
    });
});
//# sourceMappingURL=store.module.spec.js.map